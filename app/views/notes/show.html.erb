<h2><%= @note.city %></h2>
<p><%= @note.message %></p>


<%= link_to "Delete Note",
            note_path(@note),
            method: :delete,
            data: { confirm: "Are you sure?" } %>

            <%= link_to "Edit Note", edit_note_path(@note) %>

            <%= link_to "Back", :back %>


            
